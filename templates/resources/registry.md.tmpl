---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Example Usage
We need to configure the provider
{{ tffile (printf "examples/provider/provider.tf")}}

We need a namespace where the registry will be deployed:
{{ tffile (printf "examples/resources/nexaa_namespace/resource.tf")}}

Then we can create a {{ .Name }} resource
{{ tffile (printf "examples/resources/%s/resource.tf" .Name)}}

{{ .SchemaMarkdown | trimspace }}

## Import

Import existing resources is supported.

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/bin/bash
terraform import nexaa_container "namespace/container_name"
```